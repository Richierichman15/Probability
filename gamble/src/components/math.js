import react from "react";

const math = () => {
    const factorial = n => {
        if ( n === 0 || n === 1) return 1;
        let result = 1;
        for (let i = 2; i <= n; i++) {
            return *= i;
        }
        return result
    }
    return(
        <div>
            <h3>Probability</h3>
            <input></input>
            <button></button>
        </div>
    )
}

export default math;